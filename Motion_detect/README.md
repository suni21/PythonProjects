# Detecting motion and moving objects

This project was about detecting motion.

# Installation steps:

Clone the repository for that create a repo in github then copy the url and then with the help of command -git clone 
https://github.com/suni21/Motion_detect.git we clone it

Then open the folder Python with command -cd Python

Then install all the required libraries such as numpy, opencv, matplotlib then freeze it using command- pip3 freeze > requirements.txt

Upload the video.

Then run the program(code)

Save the file

Then finally use add,commit,push.  